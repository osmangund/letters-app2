/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export",
  // basePath: "/nextjs-lettersapp",
  // images: {
  //   unoptimized: true,
  // },
}

export default nextConfig
